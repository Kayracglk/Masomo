using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum AnimalState
{
    run, feed, chaseToPlayer, walk, idle,sleep,
}

public enum AnimalType
{
    Deer, Rabbit, Fox, Boar, Bird, Bear
}
